{
  "name": "bklar",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run --filter './packages/*' build",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "format": "prettier --write ./packages"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "lefthook": "^2.1.9",
    "prettier": "^3.9.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
